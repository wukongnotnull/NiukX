# SNS 公开版本清单

自托管实例用这份 JSON 检查是否有新版本。

- 文件：[`latest.json`](./latest.json)
- 地址：https://raw.githubusercontent.com/wukongnotnull/sns-releases/main/latest.json

源码仓库是私有的。发新版时更新 `latest.json` 的 `version`、`releasedAt`、`notes`、`image`。
